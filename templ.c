#include <stdio.h>

int main(void)
{
	int g, h;

	printf("input g: ");
	scanf("%d", &g);
	printf("input h: ");
	scanf("%d", &h);

	printf("before: g = %d, h = %d\n", g, h);
	// write here --- begin ---
	
	// write here ---  end  ---
	printf("after:  g = %d, h = %d\n", g, h);

	return 0;
}
